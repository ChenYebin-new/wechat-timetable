# 更新记录

本项目的重要变化记录在此文件中。

## [Unreleased]

### Added

- 添加微信开发者工具生成的原生 TypeScript 基础项目。
- 添加第一版功能、开发、测试和上线指南。
- 添加 GitHub 分支、提交、Pull Request 和敏感信息管理规范。
- 添加仓库说明和忽略规则。
- 添加第一版开发任务分工，明确 Liu 的开发责任和 Yebin 的审核、验收及发布责任。
- 添加第一版设计方案，作为评审与实现依据。
- 添加 V1.0.1 与 V1.1 合并开发任务书，明确数据备份恢复、教学周、数据迁移、验收与分阶段提交规则。
- 实现第一版课表：七天周课表（含左侧九节课时段、跨行课程卡片）、课程新增/编辑/删除（删除二次确认）、同天同节次冲突检查、本地 Storage 保存。
- 课表网格采用 flex + 绝对定位，以兼容 iOS 等设备（不使用 CSS Grid）。
- 实现 V1.0.1 数据备份与恢复：课表 JSON 导出、粘贴导入与严格校验、导入预览、覆盖/合并、最近自动备份及恢复。
- 增加数据版本识别与迁移入口骨架：当前仅读写 V1，未知更高版本拒绝导入且不降级写回。
- 增加不依赖第三方测试库的备份服务自动化测试，覆盖导入校验、合并统计、版本保护和写入失败恢复。
- 实现 V1.1 学期与教学周：学期设置（第一教学周星期一 + 总周数 1–30）、当前教学周计算与上/下/指定周导航、课表按所选周过滤，以及未设置学期/不在教学周内的状态提示。
- 课表支持左右跟手滑动切换教学周，保留上一周/下一周按钮和直接选周入口；多选时锁定切周以避免跨周误操作。
- 课程增加周次模式（全部周/单周/双周/指定周次），编辑页支持正确回显与多选周次；冲突判断升级为星期、节次、周次三者都重叠。
- 课表数据升级为 V2（schemaVersion:2 + term + weekMode/weeks），实现 V1→V2 迁移：读取 V1 → 提示设置学期 → 自动备份 → 逐门迁移 → 内存校验 → 一次性写入并复核；用户取消或失败时原数据不变。
- 备份兼容 V2：导出包含完整学期与周次；导入识别 V1 备份并要求先设置学期；未知更高版本拒绝且不降级写回。
- 增加课表网格长按多选新增：空白格按住 1.2 秒进入选择，可跨星期点选，同日连续节次自动合并后统一填写课程信息。
- 增加多时段课程组：支持整体编辑和重新选择时段、单时段编辑后自动拆分，以及按单时段或整门课程删除。
- 课表数据升级为 V3，以 `groupId` 关联同一课程的多个时段；自动备份并迁移 V2 数据，导入兼容 V1/V2/V3。
- 增加格子归并、课程组原子保存、整组更新、单时段拆分、整组删除、V2→V3 迁移和备份课程组校验测试。
- 新增统一设置首页，将学期设置、课程时间设置和数据管理集中到同一入口，课表周次栏继续保留学期快捷入口。
- 新增全局课程时间设置：支持 1–14 节、默认课时时长、默认课间休息、按规则重新生成，以及逐节自定义起止时间。
- 课表网格、课程编辑器、时段多选、课程校验和滑动课表高度改为读取动态节次。
- 课表数据升级为 V4，在根数据和备份中保存完整课程作息；V1–V3 数据兼容迁移为默认九节作息，作息不同的备份明确拒绝合并。
- 增加课程作息生成、边界校验、缩减保护、V3→V4 迁移与回滚、V4 备份覆盖恢复和设置页结构测试。
- 课程时间设置改为实时联动：调整全局课时时长或课间休息后立即刷新全部非自定义节次，修改单节开始时间会自动计算结束时间并重排后续课程。
- 增加手动开始时间锚点和单节自定义时长；午休等锚点保持固定，新规则与锚点冲突或导致课程跨越午夜时拒绝修改并给出具体提示。
- 课表数据升级为 V5，V4 作息会自动推断第一节起点、手动时间锚点和自定义时长，备份、覆盖、恢复和合并校验同步支持 V5。
- 增加全局规则联动、单节时间平移、锚点冲突、V4→V5 推断和 V5 备份兼容测试。
- 增加严格 V5 快照校验、显式 Storage 读取状态、课程草稿类型和集中版本识别，为后续本地功能扩展建立稳定数据边界。
- 固定 TypeScript 5.9.3，增加 `typecheck`、`test:coverage`、`check` 脚本和 GitHub Actions 检查。
- 课表首页支持通过微信右上角菜单发送给好友，并在支持的客户端分享到朋友圈；分享的是小程序入口，不携带本机课表数据。

### Changed

- 将项目说明、设计、任务与协作文档统一整理到 `docs/` 目录，并更新 README 文档入口。
- 优化课表主页的信息层级与空间分布：增加今日课程概览，放大节次行高，并调整星期栏、时间栏和课程卡片的圆角块面布局。
- 进一步增加课表节次行高以减少页面底部留白，并移除课程编辑页星期选项前的数字序号。
- 备份预览和合并结果同时显示课程组数与时段数；合并改为按 `groupId` 整组处理。

### Fixed

- 修复课程执行一次编辑后再次点击不再询问编辑范围的问题；现在每次点击都会重新选择“仅编辑本时段”或“编辑整门课程”。
- 修复 V1.1.1 审查中发现的单双周误判冲突、非法学期日期、不同学期备份错误合并、V1 最近备份无法恢复及未知更高版本被降级迁移的问题。
- 更新并扩充 V2 自动化测试，覆盖教学周边界、V1→V2 迁移、周次冲突、备份恢复、不同学期合并和写入失败回滚。
- 阻止 V1.0.1 对未知更高版本课表执行编辑、删除、导出、覆盖、合并或恢复，避免旧结构写坏未来版本数据。
- 严格校验备份时间、整数星期与节次、课程 ID、色板、时间戳及可选字段，并只写入校验后的规范化课程。
- 完善覆盖、合并和恢复失败时的回滚与提示，恢复失败时保留原来的最近备份。
- 合并完成后显示新增、重复、冲突和最终课程数量，并修正覆盖、恢复确认文案。
- 修复临时 Storage 读取失败可能被当成空课表继续写入的问题；读取失败或当前 V5 损坏时保持只读。
- 修复非法周次在无关写操作中被静默清洗、非色板合法颜色无法完成导出导入闭环，以及多时段课程只导入部分时段的问题。
- 修复查看非当前教学周时概览误称“今天”以及按时段数误报课程门数的问题。

### Notes

- 暂不包含：待办事项、文件/教务导入、账号登录、云同步、消息提醒。
- `project.config.json` 当前使用测试 AppID，正式上传前需替换为正式 AppID。
