# 第一版开发任务分工

> 项目：微信课表小程序
> 目标版本：第一版
> 开发负责人：Liu
> 需求审核与发布负责人：Yebin

## 1. 第一版目标

第一版需要交付一个可以实际使用的个人课表小程序。用户可以在一张周课表中查看周一至周日的课程，手动新增、编辑和删除课程，课程数据保存在微信本地 Storage 中。

第一版完成后，应满足以下结果：

- 同一页面展示周一至周日七列课表。
- 左侧固定展示九节课及对应时间。
- 课程卡片根据星期和节次出现在正确位置。
- 连续多节课程显示为一张跨行课程卡片。
- 用户可以手动新增、编辑和删除课程。
- 课程数据在关闭并重新打开小程序后仍能读取。
- 同一天的节次发生重叠时拒绝保存，并给出明确提示。
- 空课表状态能够引导用户添加第一门课程。

## 2. 人员分工

| 人员 | 职责 |
| --- | --- |
| Liu | 负责第一版全部编码、开发自测、相关文档更新，并通过一个 Pull Request 提交完整版本。 |
| Yebin | 不承担编码；负责确认需求、审核 Pull Request、完成最终真机验收，以及上传和发布微信版本。 |

Liu 不负责把代码上传到微信公众平台、提交微信审核或发布正式版本。GitHub 代码合并和微信版本发布均由 Yebin 最终确认。

## 3. 固定课时

第一版固定使用九节课，不提供作息时间设置功能：

| 节次 | 时间 |
| --- | --- |
| 第 1 节 | 08:00–08:50 |
| 第 2 节 | 09:00–09:50 |
| 第 3 节 | 10:00–10:50 |
| 第 4 节 | 11:00–11:50 |
| 第 5 节 | 14:00–14:50 |
| 第 6 节 | 15:00–15:50 |
| 第 7 节 | 16:00–16:50 |
| 第 8 节 | 17:00–17:50 |
| 第 9 节 | 18:00–18:50 |

课程默认每周重复。第一版不计算学期、教学周、单双周、调课和停课。

## 4. Liu 的开发任务

### 4.1 项目与页面

- 使用仓库中现有的原生微信小程序、TypeScript、WXML 和 WXSS。
- 将微信官方模板页面替换为课表主页和课程编辑页。
- 正确注册页面路径，清理不再使用的演示入口和演示内容。
- 不引入跨端框架、UI 组件库、云开发或后端服务。

### 4.2 课表主页

- 页面同时显示周一至周日七列，以及左侧九节课的节次和时间。
- 课程卡片按照星期、开始节次和结束节次计算位置与高度。
- 单节课和连续多节课都必须正确显示。
- 七天在手机宽度内同时可见，不依赖横向滚动完成主要查看。
- 长课程名不能撑坏网格；空间不足时可以省略次要信息，编辑页中显示完整内容。
- 空课表时显示明确的新增课程入口。
- 点击已有课程进入编辑页面。

### 4.3 课程编辑

课程至少包含以下字段：

- 课程 ID。
- 课程名称。
- 星期。
- 开始节次和结束节次。
- 教师，可选。
- 教室，可选。
- 课程颜色。
- 创建时间和更新时间。

编辑页面需要支持：

- 新增课程。
- 修改已有课程。
- 删除课程，删除前需要二次确认。
- 校验课程名称、星期和节次等必填信息。
- 校验节次范围必须在 1–9 之间。
- 校验开始节次不能晚于结束节次。
- 保存、编辑或删除后，返回课表主页立即显示最新结果。

### 4.4 冲突检查

两门课程在同一天且节次区间存在重叠时视为冲突。例如，第 2–4 节与第 4–5 节冲突，第 2–3 节与第 4–5 节不冲突。

- 新增课程发生冲突时拒绝保存。
- 编辑课程时，冲突检查需要排除当前课程自身。
- 提示内容需要让用户知道冲突发生在哪一天、哪些节次。

### 4.5 本地存储

- 使用微信本地 Storage 保存课程，不接入云端数据库。
- 数据根结构包含 `schemaVersion: 1` 和课程列表。
- 课程读取、新增、更新和删除逻辑集中管理，不分散在多个页面中直接操作 Storage。
- 首次启动、没有数据和本地数据异常时，页面不能白屏或崩溃。

### 4.6 视觉与适配

- 使用清晰的蓝绿色基础主题、圆角课程卡片和轻量半透明层。
- 优先保证文字可读性和课程位置清楚，不使用会显著降低对比度的复杂背景。
- 不复制其他小程序的名称、Logo、照片或品牌元素。
- 检查窄屏、顶部安全区域和输入键盘遮挡问题。

## 5. GitHub 交付方式

Liu 使用一个功能分支和一个完整 Pull Request 交付第一版：

```powershell
git switch main
git pull --ff-only
git switch -c feature/v1-timetable
```

开发过程中可以创建多个语义明确的提交，但不要把无关改动混入该分支。完成后将 `feature/v1-timetable` 推送到 GitHub，并向 `main` 发起一个 Pull Request。

Pull Request 必须说明：

- 实现了哪些功能。
- 修改了哪些页面或模块。
- 实际测试过的操作路径和结果。
- 微信开发者工具是否编译通过，Console 是否有新增错误。
- 是否完成手机预览。
- 本地存储结构及其版本。
- README 和 CHANGELOG 的更新情况。
- 尚未解决的问题或已知限制。

Liu 不自行合并 Pull Request。Yebin 完成审核和验收后决定是否合并。

## 6. Liu 的验收清单

提交 Pull Request 前，Liu 必须逐项检查：

- [ ] 空数据首次启动正常，并显示新增课程引导。
- [ ] 周一至周日都能添加课程。
- [ ] 单节课程的位置和高度正确。
- [ ] 连续多节课程显示为一张完整卡片。
- [ ] 课程可以新增、编辑和删除，删除前有确认步骤。
- [ ] 关闭并重新打开小程序后，课程数据仍然存在。
- [ ] 同一天存在重叠节次时不能保存。
- [ ] 相邻但不重叠的课程可以保存。
- [ ] 编辑课程时不会与课程自身产生冲突。
- [ ] 长课程名、课程较多和窄屏设备不会破坏布局。
- [ ] 微信开发者工具编译通过，Console 没有新增错误。
- [ ] 至少完成一次手机预览。
- [ ] README 和 CHANGELOG 只记录已经实现并验证的功能。
- [ ] 没有提交个人配置、依赖目录或敏感信息。

Yebin 在合并前完成最终需求检查和真机验收，正式微信版本由 Yebin 上传和发布。

## 7. 第一版不包含的内容

以下内容不属于 Liu 本次任务，不应在该 Pull Request 中提前实现：

- 待办事项。
- CSV、Excel、图片或教务系统课表导入。
- 学期、教学周、单双周、调课和停课。
- 用户账号、微信登录和多设备同步。
- 云开发、后端服务和数据库。
- 微信订阅消息和上课提醒。
- 作息时间自定义。
- 自定义背景、主题商城、心情、打卡和工具箱。
- `assets/branding/` 中由 Yebin 在微信平台实际使用和维护的小程序图标。该目录不提交 GitHub，也不属于 Liu 的修改范围。

如果开发中发现必须扩大范围才能继续，Liu 应先在 Pull Request 或项目沟通中说明原因，由 Yebin 确认后再调整任务，不能自行扩大第一版范围。

## 8. 完成交付的定义

只有同时满足以下条件，第一版开发任务才算完成：

1. Liu 已完成本文件规定的功能和自测。
2. `feature/v1-timetable` 已创建 Pull Request，说明和测试证据完整。
3. Yebin 已完成代码审核和真机验收。
4. 审核意见已经处理，Pull Request 由 Yebin 确认合并。

代码合并到 `main` 不代表已经发布到微信。微信体验版、审核和正式发布由 Yebin 另行执行。
