# 微信课表小程序

一个使用原生微信小程序和 TypeScript 开发的个人课表工具。

已实现：基于原生微信小程序 + TypeScript 的七天周课表，支持课程新增、编辑、删除、冲突检查、数据备份与恢复（V1.0.1）、学期与教学周（V1.1）、左右滑动切周、课表网格长按多选新增，以及全局课程时间设置。数据保存在微信本地 Storage。

## 第一版功能（已实现）

- 展示周一至周日七列课表，左侧按全局作息显示 1–14 节课程及对应时间。
- 课表主页采用“今日安排概览 + 一周课表”的纵向布局，七天保持同屏显示。
- 连续多节课程显示为一张跨行课程卡片。
- 手动新增、编辑和删除课程（删除前二次确认）。
- 检查同一天、同一节次的课程冲突，编辑时排除课程自身。
- 使用微信本地 Storage 保存课程，关闭重进后数据仍在。
- 空课表时展示新增课程引导。
- 数据管理（V1.0.1）：导出课表为 JSON 文本、粘贴并校验导入、覆盖或按课程组原子合并、恢复最近自动备份。
- 学期与教学周（V1.1）：设置第一教学周星期一与学期总周数（1–30）；课表顶部显示当前"第 N 周"，支持左右滑动课表、上一周/下一周按钮和直接选周；课表只显示所选周实际有课的课程。进入长按多选后会暂时锁定切周，避免误选到其他周。
- 课程周次（V1.1）：每门课程可选全部周、单周、双周或指定周次。
- 冲突判断（V1.1 升级）：星期、节次、周次三者都重叠才算冲突。
- 支持长按课表空白格 1.2 秒进入多选，可跨星期选择多个时段；同一天连续节次自动合并，并作为同一门课程统一填写信息。
- 点击课程时每次都会重新询问编辑范围；多时段课程支持整体编辑、重新选择时段，或仅编辑当前时段并自动拆分。删除时可选择仅删除当前时段或整门课程。
- 课表主页提供统一“设置”入口，可进入学期设置、课程时间设置和数据管理；原周次栏中的“学期”快捷入口继续保留。
- 课程时间设置支持全局课时时长、课间休息、每日课程数和逐节起止时间。修改全局规则会立即重排下方课程；手动开始时间作为午休等固定锚点保留，手动结束时间作为该节专属时长保留，发生锚点冲突时不会覆盖原设置。
- 暂不包含：待办事项、文件或教务系统导入、账号登录、云同步、消息提醒。

## 项目目录

```text
WechatCode/
├─ wechat-timetable/                 # 微信开发者工具项目
│  ├─ miniprogram/                   # 小程序源代码
│  │  ├─ pages/timetable/            # 课表主页
│  │  ├─ pages/settings/             # 统一设置页
│  │  ├─ pages/period-settings/      # 课程时间设置页
│  │  ├─ pages/period-time-edit/     # 单节课程时间编辑页
│  │  ├─ pages/course-edit/          # 新增/编辑课程页
│  │  ├─ pages/slot-select/          # 多时段重新选择页
│  │  ├─ pages/data-manage/          # 数据管理页（V1.0.1）
│  │  ├─ pages/term-settings/        # 学期设置页（V1.1）
│  │  ├─ components/timetable-grid/  # 七天课表网格
│  │  ├─ models/                     # 课程与备份数据模型
│  │  ├─ services/                   # 本地存储与备份服务
│  │  ├─ utils/                      # 布局与课程校验工具
│  │  └─ constants/                  # 星期、课时、色板
│  ├─ tests/                          # 备份与恢复自动化测试
│  ├─ typings/                       # 微信 API 类型定义
│  ├─ package.json
│  ├─ project.config.json            # 团队共享项目配置
│  └─ tsconfig.json
├─ docs/                              # 项目说明、设计、任务与协作文档
│  ├─ 第一版开发任务分工.md
│  ├─ 第一版设计方案.md
│  ├─ V1.0.1与V1.1开发任务书.md
│  ├─ 微信课表小程序开发与上线指南.md
│  └─ 微信课表小程序协作开发规范.md
├─ README.md
└─ CHANGELOG.md
```

`project.private.config.json` 是每位开发者自己的微信开发者工具配置，不提交到仓库。

## 本地运行

1. 克隆仓库。
2. 在 `wechat-timetable` 目录安装依赖：

   ```powershell
   cd wechat-timetable
   npm install
   ```

3. 打开微信开发者工具，导入 `wechat-timetable` 目录。
4. 使用项目成员自己的微信扫码登录并编译。

如果需要使用正式 AppID 预览或真机调试，仓库管理员还需要在微信公众平台将协作者添加为项目成员。GitHub 仓库权限与微信小程序权限分别管理。

## 数据备份与恢复

1. 在课表主页点击“设置”，再进入“数据管理”。
2. 点击“复制课表 JSON”，把备份文本保存到可信位置。
3. 恢复时将完整 JSON 粘贴到输入框，点击“解析并预览”。
4. 核对预览中的课程、重复和冲突数量，再选择覆盖或合并。

覆盖、合并、恢复、数据迁移和课程作息保存前都会保留当前课表作为最近自动备份。当前课表使用 V5 数据结构：`groupId` 关联同一门课程的多个上课时段，`periodSettings` 同时保存全局作息规则、第一节起点、手动锚点、单节自定义时长和完整计算结果。V4 数据会从原有时间推断并保留午休锚点与单节时长，V1–V3 数据会生成默认九节作息。格式错误、字段非法、Storage 读取失败或版本不兼容时会停止写入并保留原数据；V1–V4 备份均可兼容导入。合并以整门课程为单位，任一时段重复或冲突时整组跳过，避免导入不完整课程。新版备份只有与当前课表采用相同学期和完全一致的课程作息时才能合并，不一致时请使用覆盖导入；未知更高版本会停止修改且不会降级写回。

格子归并、课程组事务、课程作息、备份、迁移、教学周和周次冲突的自动化测试使用 Node.js 24 自带能力；TypeScript 固定为 5.9.3。完整检查会先执行真实类型检查，再运行全部测试：

```powershell
cd wechat-timetable
npm run check
```

需要单独查看覆盖率时运行 `npm run test:coverage`。

提交或发布前仍需在微信开发者工具与真机完成平台验收：冷启动首次设置学期、切换教学周、多时段课程整体/单时段编辑、备份解析与整组合并、V1–V4 迁移，以及模拟 Storage 读取失败后的只读提示；同时确认开发者工具控制台没有新增错误。Node.js 测试不能替代这些平台检查。

## 协作方式

- `main` 保存已经检查、可以继续测试的代码。
- 每项功能或修复使用独立分支。
- 开发完成后通过 Pull Request 审查并合并。
- 只暂存当前任务涉及的明确文件，不使用 `git add .` 或 `git add -A`。
- 用户可见功能、依赖、启动方式或重要修复发生变化时，同步更新 README 和 `CHANGELOG.md`。

第一版的负责人、交付目标和验收条件见[第一版开发任务分工](./docs/第一版开发任务分工.md)。下一阶段的数据备份恢复、学期与教学周开发要求见[V1.0.1 与 V1.1 开发任务书](./docs/V1.0.1与V1.1开发任务书.md)。详细协作规则见[微信课表小程序协作开发规范](./docs/微信课表小程序协作开发规范.md)，开发、测试和上线步骤见[微信课表小程序开发与上线指南](./docs/微信课表小程序开发与上线指南.md)。

## 敏感信息

不要提交或发布以下内容：

- AppSecret、Access Token 或云开发密钥。
- 微信代码上传私钥。
- `project.private.config.json`。
- 管理员二维码、登录凭证和备案材料。
- 与开发无关的个人信息。
